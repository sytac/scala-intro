package exercise2

object SumNElements {
  /**
    * Calculate the sum of the first n elements of a List.
    * The solution should work also for very long lists, so it should be implemented in a tail-recursive fashion
    */

  def calculate(list: List[Int], untilElement: Int): BigInt = ???
}
