package exercise4

object ListContains {
  def contains(list: List[Int], element: Int): Boolean = ???  // Implement a function that checks via pattern matching
                                                              // and tail recursion if an element is present in a List.
}
