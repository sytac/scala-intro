package exercise4

object ListContains {

  /**
    * Implement a function that checks via pattern matching
    * and tail recursion if an element is present in a List.
    */
  def contains(list: List[Int], element: Int): Boolean = ???
}
